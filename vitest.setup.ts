// Extiende `expect` con los matchers de Testing Library (toBeInTheDocument, etc.)
// para los tests de componentes (Tier 2). Inofensivo para los tests puros.
import "@testing-library/jest-dom/vitest";
